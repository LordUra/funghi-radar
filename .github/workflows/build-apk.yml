name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Capacitor
        run: npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Init Capacitor
        run: |
          npx cap init "Funghi Radar" "com.funghiradar.app" --web-dir www || true

      - name: Add Android platform
        run: |
          npx cap add android || true
          npx cap sync android

      - name: Fix AndroidManifest permissions
        run: |
          FILE=android/app/src/main/AndroidManifest.xml
          if ! grep -q "INTERNET" $FILE; then
            sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/' $FILE
          fi

      - name: Generate keystore
        run: |
          keytool -genkeypair \
            -v \
            -keystore funghi-radar.keystore \
            -alias funghi-radar \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass funghiradar123 \
            -keypass funghiradar123 \
            -dname "CN=FunghiRadar, OU=Dev, O=FunghiRadar, L=Roma, ST=Lazio, C=IT"

      - name: Build Release APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Sign APK
        run: |
          cd android/app/build/outputs/apk/release
          jarsigner \
            -verbose \
            -sigalg SHA256withRSA \
            -digestalg SHA-256 \
            -keystore $GITHUB_WORKSPACE/funghi-radar.keystore \
            -storepass funghiradar123 \
            -keypass funghiradar123 \
            app-release-unsigned.apk \
            funghi-radar
          zipalign -v 4 app-release-unsigned.apk funghi-radar-signed.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: funghi-radar-release
          path: android/app/build/outputs/apk/release/funghi-radar-signed.apk
